(function () {
    "use strict";

    angular.module('app').controller('FrontController', FrontController);

    FrontController.$inject = ['$scope'];

    function FrontController($scope) {
        var front = this;

        init();
        function init() {
        }

    }

})();